import { Entity } from 'typeorm'

@Entity()
export class MateriaisDownloads {
    // @ObjectIdColumn()
    // _id: ObjectID; // ID do download (chave primária)
    // @Column()
    // material_id: number; // ID do material (chave estrangeira)
    // @Column()
    // usuario_id: number; // ID do usuário (chave estrangeira)
    // @Column()
    // baixado_em: Date; // Data do download
}
